# Match Rust polars 0.52 (required for datui Python bindings / LazyFrame DSL_SCHEMA_HASH)
polars>=1.44.1,<1.45

# for building sample data
numpy
pyarrow
fastavro
openpyxl
jinja2

# for running the pre-commit hooks
pre-commit

# Wheel build deps: requirements-wheel.txt (Linux/macOS), requirements-wheel-windows.txt (Windows)
